# Возможности плагина Jira QA Helper

## Основной функционал

### ✅ Автоматическое создание QA-подзадач
- Создание двух подзадач одним кликом
- Автоматическое наследование названия родительской задачи
- Добавление префиксов `[Тестирование]` и `[Документация]`
- Использование типа задачи QA-subtask (или fallback на обычный subtask)

### ✅ Конвертация подзадач в независимые задачи
- Поиск всех QA-подзадач по префиксам
- Автоматическое изменение типа с subtask на обычную задачу
- Создание связи "relates to" с бывшей родительской задачей
- Сохранение всех полей и истории задачи

## Технические детали

### Используемые технологии
- **Chrome Extension API**: Manifest V3 (последняя версия)
- **Jira REST API v3**: Официальный API для работы с задачами
- **Content Scripts**: Для добавления UI элементов на страницу Jira
- **MutationObserver**: Для отслеживания динамических изменений DOM

### Поддерживаемые платформы
- Jira Cloud (`*.atlassian.net`)
- Jira Server (кастомные домены)
- Jira Data Center

### Интеграция с Jira
Плагин использует следующие API endpoints:
- `GET /rest/api/3/issue/{issueKey}` - получение данных задачи
- `POST /rest/api/3/issue` - создание новой задачи/подзадачи
- `PUT /rest/api/3/issue/{issueKey}` - обновление задачи (конвертация)
- `POST /rest/api/3/issueLink` - создание связей между задачами
- `GET /rest/api/3/issue/createmeta` - получение доступных типов задач

### Безопасность
- Использование credentials: 'include' для безопасной передачи cookies
- Все операции выполняются от имени текущего пользователя
- Требуются соответствующие права в Jira
- Нет хранения паролей или токенов

## UI/UX возможности

### Визуальный интерфейс
- Две кнопки с градиентным дизайном
- Emoji-иконки для наглядности (➕ и 🔄)
- Плавные анимации при наведении
- Адаптивный дизайн (responsive)

### Уведомления
- Всплывающие уведомления о результатах операций
- Цветовая индикация: успех (зеленый), ошибка (красный), предупреждение (желтый)
- Автоматическое скрытие через 5 секунд
- Отображение ключей созданных задач

### Совместимость с Jira UI
- Работает с современным Jira Cloud интерфейсом
- Поддержка классического интерфейса Jira
- Автоматическое определение места для вставки кнопок
- Не конфликтует с другими плагинами Jira

## Дополнительные функции

### Автоматическое обновление
- MutationObserver отслеживает изменения DOM
- Автоматическое добавление кнопок при навигации
- Поддержка SPA (Single Page Application) навигации Jira
- Обработка динамически загружаемого контента

### Обработка ошибок
- Детальные сообщения об ошибках
- Логирование в консоль браузера для отладки
- Graceful fallback при отсутствии типа QA-subtask
- Проверка прав доступа перед выполнением операций

### Производительность
- Минимальное влияние на производительность страницы
- Асинхронные операции (async/await)
- Отсутствие блокирующих операций
- Оптимизированные селекторы DOM

## Планы развития (future roadmap)

### Потенциальные улучшения
- [ ] Настраиваемые префиксы подзадач
- [ ] Выбор типа задачи из списка
- [ ] Массовое создание подзадач для нескольких задач
- [ ] Экспорт/импорт настроек
- [ ] Статистика использования
- [ ] Поддержка других типов связей (blocks, depends on, etc.)
- [ ] Шаблоны для описания подзадач
- [ ] Автоматическое назначение исполнителей

### Интеграция с другими инструментами
- [ ] Интеграция с Confluence
- [ ] Экспорт в CSV/Excel
- [ ] Webhook уведомления
- [ ] Slack/Teams интеграция

## Известные ограничения

1. **Права доступа**: Требуются права на создание подзадач и изменение типов задач
2. **Типы задач**: В проекте должен существовать тип subtask
3. **Конвертация**: Некоторые кастомные поля могут не переноситься при конвертации
4. **Браузеры**: Протестировано только в Chrome/Chromium (работает в Edge, Brave, Opera)
5. **Jira версии**: Оптимизировано для Jira Cloud, может требовать доработок для старых версий Server

## Лицензия

MIT License - свободное использование и модификация
