# Jira QA Helper

Chrome-плагин для автоматизации работы с QA-подзадачами в Jira.

## Возможности

1. **Создание QA-подзадач**: Одним кликом создаются две подзадачи типа QA-subtask:
   - `[Тестирование] Название родительской задачи`
   - `[Документация] Название родительской задачи`

2. **Конвертация подзадач**: Автоматическая конвертация QA-подзадач в обычные задачи с линкованием к бывшей родительской задаче через "relates to"

## Установка

### Способ 1: Установка в режиме разработчика (рекомендуется)

1. Откройте Chrome и перейдите по адресу `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `/Users/khoroshavin/jira-qa-helper`
5. Плагин установлен и готов к работе!

### Способ 2: Создание .crx файла

1. В `chrome://extensions/` нажмите "Упаковать расширение" (Pack extension)
2. Выберите папку `jira-qa-helper`
3. Будет создан .crx файл, который можно установить

## Использование

1. Откройте любую задачу в Jira (например, `https://your-company.atlassian.net/browse/PROJ-123`)
2. Под заголовком задачи появятся две кнопки:
   - **➕ Создать QA подзадачи** - создаёт две подзадачи
   - **🔄 Конвертировать подзадачи** - конвертирует существующие QA-подзадачи в задачи

3. После нажатия на любую кнопку появится уведомление о результате операции
4. Страница автоматически перезагрузится для отображения изменений

## Требования

- Google Chrome (или другой Chromium-браузер)
- Доступ к Jira Cloud (*.atlassian.net) или Jira Server
- Права на создание подзадач и изменение задач в Jira

## Технические детали

Плагин использует:
- Jira REST API v3 для работы с задачами
- Content Script для добавления кнопок на страницу
- Manifest V3 (последняя версия Chrome Extension API)

## Поддержка

Плагин работает с:
- Jira Cloud (*.atlassian.net)
- Jira Server (кастомные домены jira.*)

## Примечания

- Если тип задачи "QA-subtask" не найден в вашем проекте, плагин использует первый доступный тип subtask
- При конвертации подзадач плагин ищет задачи с префиксами `[Тестирование]` и `[Документация]`
- Для конвертации используется тип задачи "Task" или первый доступный не-subtask тип

## Устранение неполадок

**Кнопки не появляются на странице:**
- Убедитесь, что вы находитесь на странице задачи (URL содержит `/browse/`)
- Обновите страницу (F5)
- Проверьте консоль браузера (F12) на наличие ошибок

**Ошибка при создании подзадач:**
- Проверьте, что у вас есть права на создание подзадач в проекте
- Убедитесь, что тип задачи QA-subtask или другой subtask существует в проекте

**Ошибка при конвертации:**
- Проверьте, что у вас есть права на изменение типа задачи
- Убедитесь, что подзадачи с нужными префиксами существуют
